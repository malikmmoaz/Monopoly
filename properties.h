#ifndef PROPERTIES_H
#define PROPERTIES_H

using namespace std;

    // property #1 - ehab's Ghar
    void ehabGhar(int &balance, bool &posession)   {

        cout << "You have landed on Ehab's Ghar - Cognrats! You now own another uselses digital property." << endl;
        balance = balance - 10;
        posession = true;

        return;
    }

    // property #2 - osa
    void osa(int &balance, bool &posession)   {

        cout << "You have landed on OSA - Cognrats! You're Adnan Khan's Boss :wink:" << endl;
        balance = balance - 10;
        posession = true;

        return;
    }

    // property #3 - pdc
    void pdc(int &balance, bool &posession)   {

        cout << "You have landed on PDC - Cognrats! You'll eat kal ki sakht roti :(" << endl;
        balance = balance - 10;
        posession = true;

        return;
    }

    // property #4 - hss
    void hss(int &balance, bool &posession)   {

        cout << "You have landed on HSS - Cognrats! You're jobless :(" << endl;
        balance = balance - 10;
        posession = true;

        return;
    }

    // property #5 - lmbiCp
    void lmbiCp(int &balance, bool &posession)   {

        cout << "You have landed on Lmbi CP - Cognrats! You're from HSS :)" << endl;
        balance = balance - 10;
        posession = true;

        return;
    }

    // property #6 - soe
    void soe(int &balance, bool &posession)   {

        cout << "You have landed on SOE - Cognrats! No one cares :)" << endl;
        balance = balance - 10;
        posession = true;

        return;
    }

    // property #7 - lawSchool
    void lawSchool(int &balance, bool &posession)   {

        cout << "You have landed on LawSchool - Cognrats! No one knows you xD!" << endl;
        balance = balance - 10;
        posession = true;

        return;
    }

    // property #8 - hbaPlays
    void hbaPlays(int &balance, bool &posession)   {

        cout << "You have landed on HBAPlays - Cognrats! Press the bell icon!" << endl;
        balance = balance - 20;
        posession = true;

        return;
    }

    // property #9 - jawahir
    void jawahir(int &balance, bool &posession)   {

        cout << "You have landed on Jawahir - Cognrats! You've bought some jewellery :)" << endl;
        balance = balance - 20;
        posession = true;

        return;
    }

    // property #10 - bioMajor
    void bioMajor(int &balance, bool &posession)   {

        cout << "You have landed on Bio Major - Cognrats! You've killed some frog (chuss mari hai Mussab)" << endl;
        balance = balance - 20;
        posession = true;

        return;
    }

    // property #11 - junaidHaroon
    void junaidHaroon(int &balance, bool &posession)   {

        cout << "You have landed on Junaid Haroon - Cognrats! You've failed CS100 :(" << endl;
        balance = balance - 20;
        posession = true;

        return;
    }

    // property #12 - cal
    void cal(int &balance, bool &posession)   {

        cout << "You have landed on Calculus - Cognrats! You've to self study ab :(" << endl;
        balance = balance - 20;
        posession = true;

        return;
    }

    // property #13 - khokha
    void khokha(int &balance, bool &posession)   {

        cout << "You have landed on Khokha - Cognrats! Beta cocomo khtm hogaye :(" << endl;
        balance = balance - 20;
        posession = true;

        return;
    }

    // property #14 - subway
    void subway(int &balance, bool &posession)   {

        cout << "You have landed on SubWay - Cognrats! Paisai bhr kr bhi pet nhi bhra :(" << endl;
        balance = balance - 20;
        posession = true;

        return;
    }

    // property #15 - cso
    void cso(int &balance, bool &posession)   {

        cout << "You have landed on CSO - Cognrats! You haven't filled the survey!" << endl;
        balance = balance - 30;
        posession = true;

        return;
    }

    // property #16 - vcHouse
    void vcHouse(int &balance, bool &posession)   {

        cout << "You have landed on VC House - Cognrats! Ab fees nikalo :(" << endl;
        balance = balance - 30;
        posession = true;

        return;
    }

    // property #17 - cardammom
    void cardammom(int &balance, bool &posession)   {

        cout << "You have landed on Cardammon - Cognrats! Ab beta machine khrab hogaye hai :(" << endl;
        balance = balance - 30;
        posession = true;

        return;
    }

    // property #18 - zakir
    void zakir(int &balance, bool &posession)   {

        cout << "You have landed on Zakir - Cognrats! You've been served some great malai piece :)" << endl;
        balance = balance - 30;
        posession = true;

        return;
    }

    // property #19 - bhattaChowk
    void bhattaChowk(int &balance, bool &posession)   {

        cout << "You have landed on Bhatta Chowk - Cognrats! You've been served some great breakfast :yum:" << endl;
        balance = balance - 30;
        posession = true;

        return;
    }

    // property #20 - a16
    void a16(int &balance, bool &posession)   {

        cout << "You have landed on A-16 - Sykee! This is a14!" << endl;
        balance = balance - 30;
        posession = true;

        return;
    }

    // property #21 - sdsbDrRoom
    void sdsbDrRoom(int &balance, bool &posession)   {

        cout << "You have landed on SDSB DR- Shut your eyes :eyes_shut:" << endl;
        balance = balance - 30;
        posession = true;

        return;
    }

    // property #22 - lums
    void lums(int &balance, bool &posession)   {

        cout << "You have landed on LUMS - Congrats! Empty your pockets! " << endl;
        balance = balance - 40;
        posession = true;

        return;
    }

    // property #23 - macNcheese
    void macNcheese(int &balance, bool &posession)   {

        cout << "You have landed on macNcheese - Congrats! Beta khtm hogaye hai " << endl;
        balance = balance - 40;
        posession = true;

        return;
    }

    // property #24 - kohsarTiles
    void kohsarTiles(int &balance, bool &posession)   {

        cout << "You have landed on Koshar Tiles - Congrats! Buy some ceramics :cash:" << endl;
        balance = balance - 40;
        posession = true;

        return;
    }

    // property #25 - jayBees
    void jayBees(int &balance, bool &posession)   {

        cout << "You have landed on Jay Bees - Congrats! Buy some IceCream :yum::yum:" << endl;
        balance = balance - 40;
        posession = true;

        return;
    }

    // property #26 - johnnyJugnu
    void johnnyJugnu(int &balance, bool &posession)   {

        cout << "You have landed on Johnny & Jugnu - Congrats! Party hogaye :)" << endl;
        balance = balance - 40;
        posession = true;

        return;
    }

    // property #27 - Friday
    void friday(int &balance, bool &posession)   {

        cout << "Its Friday - Congrats! Batch night :)" << endl;
        balance = balance - 40;
        posession = true;

        return;
    }

    // property #28 - sse
    void sse(int &balance, bool &posession)   {

        cout << "You've landed on sse - Underground honai ka samai agaya hai " << endl;
        balance = balance - 40;
        posession = true;

        return;
    }

#endif